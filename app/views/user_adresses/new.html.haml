=render 'shared/loginheader'

.single-main-users
  .single-main-user-forms
    .single-main-user-form__titles
      住所の登録は初回のみ必要です。２回目以降は登録した住所を利用でき、変更することも可能です。
    .single-main-user-form__inputs
      = form_for @user_adress do |f|
        .field-label
          = f.label :"お名前(全角)"
          .hissu
            必須
        .single-main-user-form-fullname
          .field-inputs
            = f.text_field :first_name,placeholder: '例）山田'
          .field-inputs
            = f.text_field :last_name,placeholder: '例）彩'
        .field-label
          = f.label :"お名前カナ(全角)"
          .hissu
            必須
        .single-main-user-form-fullname
          .field-inputs
            = f.text_field :first_name_kana,placeholder: '例）ヤマダ'
          .field-inputs
            = f.text_field :last_name_kana,placeholder: '例）アヤ'
        .field-label
          = f.label :郵便番号（数字）
          .hissu
            必須
        .single-main-user-form-fullname
          .field-input
            = f.text_field :postal_code,placeholder: '例）1234567'
        .field-label
          = f.label :都道府県
          .hissu
            必須
        .single-main-user-form-fullname
          .field-input
            = f.text_field :prefectures,placeholder: '例）福岡県'
        .field-label
          = f.label :市区町村
          .hissu
            必須
        .single-main-user-form-fullname
          .field-input
            = f.text_field :city,placeholder: '例）福岡市博多区'
        .field-label
          = f.label :番地
          .hissu
            必須
        .single-main-user-form-fullname
          .field-input
            = f.text_field :city_adress,placeholder: '例）千代1-1-1'
        .field-label
          = f.label :建物名（任意）
        .single-main-user-form-fullname
          .field-input
            = f.text_field :building,placeholder: '例）山側ビル 101'
        .field-label
          = f.label :電話番号
          .hissu
            必須
        .single-main-user-form-fullnames
          .field-inputsss
            = f.telephone_field :phone_number,placeholder: '例）09012345678'
        .actionss
          = f.submit "登録する", class: 'btn'
=render 'shared/loginfooter'


